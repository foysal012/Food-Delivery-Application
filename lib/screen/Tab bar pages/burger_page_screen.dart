import 'package:flutter/material.dart';

class BurgerPageScreen extends StatelessWidget {
  const BurgerPageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.yellow,
      ),
    );
  }
}
