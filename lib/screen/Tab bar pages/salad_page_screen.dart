import 'package:flutter/material.dart';

class SaladPageScreen extends StatelessWidget {
  const SaladPageScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.pink,
      ),
    );
  }
}
